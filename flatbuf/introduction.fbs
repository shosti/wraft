namespace WRaft.Introduction;

table Join{
 id:string (required);
 session_id:string (required);
}

table SessionStatus{
 id:string;
 online:[string];
}

union Command { Join, SessionStatus }

table Message{
 command:Command (required);
}

root_type Message;
